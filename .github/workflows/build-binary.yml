name: Build Binary Image

on:
  push:
    branches: [master]
    paths: ['Dockerfile.binary']
  workflow_dispatch:

permissions:
  packages: write
  contents: read

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v5

      - name: Log in to GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and push ckpool binary image
        uses: docker/build-push-action@v7
        with:
          context: .
          file: Dockerfile.binary
          push: true
          tags: |
            ghcr.io/bitcoincash1/elopool-bch:latest
            ghcr.io/bitcoincash1/elopool-bch:${{ github.sha }}
          labels: |
            org.opencontainers.image.source=https://github.com/BitcoinCash1/bch-elopool-startos
